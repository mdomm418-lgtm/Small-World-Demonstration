package SmallWorld;

/**
 * Represents a single visual node in the graph.
 * Holds coordinate data for rendering.
 */
public class Node {
    public int x;
    public int y;
    
    public Node(int x, int y) {
        this.x = x;
        this.y = y;
    }
}